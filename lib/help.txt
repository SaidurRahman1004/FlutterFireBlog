lib/
├── main.dart                 # অ্যাপের প্রবেশদ্বার
├── core/
│   ├── constants.dart        # রঙ, স্ট্রিং ইত্যাদি ধ্রুবক
│   └── theme.dart            # অ্যাপের থিম
├── models/
│   └── post_model.dart       # একটা পোস্ট কেমন হবে, তার নকশা
├── screens/
│   ├── home_screen.dart      # হোম পেজ, যেখানে সব পোস্ট দেখাবে
│   ├── login_screen.dart     # লগইন পেজ
│   ├── signup_screen.dart    # সাইন আপ পেজ
│   └── add_edit_post_screen.dart # নতুন পোস্ট লেখা বা এডিট করার পেজ
├── services/
│   ├── auth_service.dart     # ইউজার লগইন/সাইনআপ/সাইনআউট এর কাজ
│   ├── firestore_service.dart # পোস্ট রিড/রাইট/আপডেট/ডিলিট এর কাজ
│   └── storage_service.dart  # ছবি আপলোডের কাজ
└── widgets/
    ├── post_card_widget.dart # একটা পোস্টকে কেমন দেখাবে, তার ডিজাইন
    └── custom_button.dart    # যদি কাস্টম বাটন লাগে (ঐচ্ছিক)



.............................................................................................................
D:\CodesApplication\Flutter Projects\FlutterFireBlog\lib\screens\post\create_post_screen.dart
কোডসহ পূর্ণ ব্যাখ্যা (Comment + ব্যাখ্যা)
import 'package:flutter/material.dart';                       // Flutter UI widget গুলোর জন্য প্রয়োজনীয় প্যাকেজ
import 'dart:io';                                             // File সাপোর্ট (লোকাল ইমেজ ফাইল ব্যবহারের জন্য)
import 'package:cloud_firestore/cloud_firestore.dart';         // Firestore Database ব্যবহার করার জন্য
import 'package:firebase_auth/firebase_auth.dart';             // User Authentication (user uid/email) পাওয়ার জন্য
import 'package:firebase_storage/firebase_storage.dart';       // Firebase Storage (ইমেজ আপলোড করার জন্য)
import 'package:image_picker/image_picker.dart';               // গ্যালারি/ক্যামেরা থেকে ছবি বাছাই করার জন্য প্যাকেজ

// ------------------ Stateful Widget শুরু ------------------
class CratePostScreen extends StatefulWidget {                 // এই স্ক্রিন ব্যবহারকারীকে নতুন পোস্ট তৈরি করতে দেয়
  const CratePostScreen({super.key});

  @override
  State<CratePostScreen> createState() => _CratePostScreenState();
}

class _CratePostScreenState extends State<CratePostScreen> {

  final _titleController = TextEditingController();            // শিরোনাম ইনপুট ফিল্ড কন্ট্রোল করার জন্য কন্ট্রোলার
  final _ContentController = TextEditingController();          // কনটেন্ট ইনপুট ফিল্ড কন্ট্রোল করার জন্য কন্ট্রোলার
  File? _image;                                                // ব্যবহারকারী নির্বাচিত ইমেজ সংরক্ষণের জন্য ভ্যারিয়েবল
  bool _loading = false;                                       // লোডিং ইনডিকেটর দেখানোর জন্য স্টেট

  // 📸 ইমেজ বাছাই করার ফাংশন (Pick Image)
  Future<XFile?> pickeImage() async {
    final pickedImg = await ImagePicker().pickImage(
      source: ImageSource.gallery,                             // 📁 গ্যালারি থেকে ছবি বাছাই করা হবে
      imageQuality: 75,                                        // ছবির কোয়ালিটি ৭৫% করে রিসাইজ করা
    );
    if (pickedImg != null)
      setState(() {
        _image = File(pickedImg.path);                         // নির্বাচিত ছবির লোকাল পাথ সংরক্ষণ করা হচ্ছে
      });
  }

  // ☁️ Firebase Storage এ ইমেজ আপলোড + Firestore এ পোস্ট যোগ করার ফাংশন
  Future<void> uploadPost() async {
    setState(() {
      _loading = true;                                         // পোস্ট করার সময় CircularProgressIndicator দেখাবে
    });

    final user = FirebaseAuth.instance.currentUser;            // বর্তমান লগইনকৃত ইউজারের ইনফরমেশন নেওয়া হচ্ছে
    String? imageUrl;                                          // আপলোডকৃত ইমেজের URL রাখার জন্য ভ্যারিয়েবল

    // 🖼️ যদি ইউজার ইমেজ বেছে নেয়
    if (_image != null) {
      final ref = FirebaseStorage.instance.ref().child(        // Storage এর একটি ফোল্ডার রেফারেন্স তৈরি করা হচ্ছে
          'post_images/${user?.uid}_${DateTime.now().millisecondsSinceEpoch}.jpg');
      await ref.putFile(_image!);                              // ইমেজ ফাইল Firebase Storage এ আপলোড
      imageUrl = await ref.getDownloadURL();                   // আপলোড শেষে ডাউনলোড লিংক পাওয়া হচ্ছে
    }

    // 🧾 Firestore এ নতুন পোস্ট যুক্ত করা হচ্ছে
    await FirebaseFirestore.instance.collection("PostcollectionPath").add({
      'title': _titleController.text,                          // ব্যবহারকারীর ইনপুটকৃত শিরোনাম
      'content': _ContentController.text,                      // ব্যবহারকারীর ইনপুটকৃত কনটেন্ট
      'authorId': user?.uid,                                   // লগইনকৃত ইউজারের UID
      'authorName': user?.email,                               // লগইনকৃত ইউজারের ইমেইল
      'timestamp': FieldValue.serverTimestamp(),               // সার্ভার টাইমস্ট্যাম্প (পোস্টের সময়)
      'imageUrl': imageUrl,                                    // ইমেজের Firebase Storage URL
    });

    if (mounted) {                                             // চেক করা হচ্ছে স্ক্রিন এখনও মাউন্টেড কিনা
      setState(() {
        _loading = false;                                      // লোডিং বন্ধ
        Navigator.pop(context);                                // পোস্ট সম্পন্ন হলে আগের স্ক্রিনে ফিরে যাওয়া
      });
    }
  }

  // 🧱 UI তৈরি অংশ
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(title: const Text("Create Post")),      // স্ক্রিনের টপে AppBar (টাইটেল)
        body: Padding(
          padding: EdgeInsets.all(12),                         // স্ক্রিনে প্যাডিং
          child: Column(children: [
              TextField(controller: _titleController,          // শিরোনাম ইনপুট
              decoration: const InputDecoration(
                labelText: "Title", border: OutlineInputBorder(),)),
          SizedBox(height: 5,),
          TextField(controller: _ContentController,            // কনটেন্ট ইনপুট
              decoration: const InputDecoration(
                labelText: "Content", border: OutlineInputBorder(),)),
          const SizedBox(height: 10),

          // 📷 ইমেজ দেখানো বা বাছাই বাটন
          _image !=null
              ? Image.file(_image!, height: 150,)               // ইউজার ইমেজ বেছে নিলে প্রিভিউ দেখানো হচ্ছে
              : TextButton.icon(
                  onPressed: pickeImage,                       // যদি ইমেজ না থাকে, ইমেজ বাছাই বাটন দেখানো হচ্ছে
                  icon: Icon(Icons.image),
                  label: Text("Select Cover Image")),

          SizedBox(height: 20,),

          // 🔁 পোস্ট করার সময় লোডিং ইনডিকেটর বা পোস্ট বাটন
          _loading
              ? CircularProgressIndicator()                    // যদি লোডিং হয়, ঘূর্ণায়মান লোডার দেখানো হচ্ছে
              : ElevatedButton(
                  onPressed: uploadPost,                       // বাটনে চাপ দিলে uploadPost() কল হবে
                  child: Text("Post")),
    ]),
    )
    ,
    );
  }
}

🧠 এই কোডে কী করা হয়েছে — সংক্ষিপ্ত বিশ্লেষণ
বিষয়	বর্ণনা
🧩 Widget Type	এটি একটি StatefulWidget, কারণ ইমেজ, লোডিং, ইনপুট টেক্সট ইত্যাদি পরিবর্তনযোগ্য (mutable)
📸 ImagePicker	ইউজার ফোনের গ্যালারি থেকে ইমেজ নির্বাচন করতে পারে
☁️ Firebase Storage	নির্বাচিত ইমেজ Firebase Storage-এ আপলোড হয়
🔥 Firestore Database	পোস্টের তথ্য (title, content, imageUrl, user info) Firestore-এ সংরক্ষণ হয়
🔐 Firebase Auth	কে পোস্ট করেছে তা চিহ্নিত করতে ইউজারের UID ও email নেওয়া হয়
🌀 Loading State	পোস্ট করার সময় CircularProgressIndicator দেখানো হয়
🔙 Navigation	সফলভাবে পোস্ট হলে আগের স্ক্রিনে ফিরে যায়
🧱 UI অংশ	টেক্সট ফিল্ড, ইমেজ সিলেকশন বাটন, পোস্ট বাটন — সব মিলিয়ে একটি সহজ পোস্ট ক্রিয়েট স্ক্রিন তৈরি হয়েছে
💡 এই কোড ব্যবহারের ফলে যা ঘটবে

✅ ইউজার নিজের টাইটেল ও কনটেন্ট লিখে ইমেজসহ একটি Post করতে পারবে।

✅ ইমেজ Firebase Storage-এ আপলোড হবে।

✅ টেক্সট ডেটা (title, content, author info) Firestore-এ সেভ হবে।

✅ “Post” বাটন চাপলে একটি CircularProgressIndicator দেখা যাবে যতক্ষণ না পোস্ট সম্পন্ন হয়।

✅ পোস্ট তৈরি হয়ে গেলে স্ক্রিনটি Navigator.pop(context) দিয়ে আগের স্ক্রিনে ফিরে যাবে।

flutterfire_blog/
│
├─ android/
├─ ios/
├─ web/ (optional)
├─ lib/
│  ├─ main.dart
│  ├─ app.dart
│  ├─ models/
│  │   └─ post.dart
│  ├─ services/
│  │   ├─ auth_service.dart
│  │   ├─ firestore_service.dart
│  │   └─ storage_service.dart
│  ├─ providers/
│  │   └─ auth_provider.dart
│  ├─ screens/
│  │   ├─ auth/
│  │   │   ├─ login_screen.dart
│  │   │   └─ signup_screen.dart
│  │   ├─ home_screen.dart
│  │   ├─ create_edit_post_screen.dart
│  │   └─ post_detail_screen.dart
│  ├─ widgets/
│  │   ├─ post_card.dart
│  │   └─ common_widgets.dart
│  └─ utils/
│      └─ validators.dart
│
├─ pubspec.yaml
└─ README.md
............................................................................
lib/
├── main.dart             # অ্যাপের এন্ট্রি পয়েন্ট
|
├── screens/              # প্রতিটি পেজ বা স্ক্রিনের জন্য আলাদা ফাইল থাকবে
│   ├── home_screen.dart      # লগইন করার পর ব্লগ ফিড দেখাবে
│   ├── login_screen.dart     # লগইন পেজ
│   ├── signup_screen.dart    # সাইন-আপ পেজ
│   └── create_post_screen.dart # নতুন পোস্ট তৈরির পেজ
|
├── services/             # ফায়ারবেস বা অন্য ব্যাকএন্ড সম্পর্কিত কাজ করার জন্য
│   ├── auth_service.dart     # অথেনটিকেশন (লগইন, সাইন-আপ, লগ-আউট) হ্যান্ডেল করবে
│   └── firestore_service.dart # Firestore ডেটাবেস (CRUD) হ্যান্ডেল করবে
|
├── widgets/              # পুনরায় ব্যবহারযোগ্য UI কম্পোনেন্ট
│   └── blog_post_card.dart   # হোম স্ক্রিনে প্রতিটি ব্লগ পোস্ট দেখানোর কার্ড
|
└── models/               # ডেটা মডেল ক্লাস
    └── post_model.dart       # ব্লগ পোস্টের ডেটা স্ট্রাকচার কেমন হবে তা ডিফাইন করবে


    /////////////////////////////////////////////////////


    অথেনটিকেশন (Authentication)
    এই ধাপকে আমরা কয়েকটি অংশে ভাগ করব:

    অংশ ক: AuthService তৈরি করা: Firebase Authentication-এর সব লজিক (সাইন-আপ, লগইন, লগ-আউট) গুছিয়ে রাখার জন্য একটি সার্ভিস ক্লাস তৈরি করা।

    অংশ খ: UI তৈরি (লগইন ও সাইন-আপ): লগইন এবং সাইন-আপ স্ক্রিনের জন্য UI ডিজাইন (প্রোটোটাইপ) ও উইজেট ট্রি।

    অংশ গ: AuthGate তৈরি: অ্যাপটি খোলার সাথে সাথে চেক করা যে ব্যবহারকারী লগইন করা আছেন কি না। লগইন করা থাকলে হোম স্ক্রিনে পাঠানো, না থাকলে লগইন স্ক্রিনে পাঠানো।
    ///////////////////////////////////////////

////////////////////////D:\CodesApplication\Flutter Projects\FlutterFireBlog\lib\services\auth_service.dart////////////////////////
FirebaseAuth.instance: এটি দিয়ে আমরা Firebase Auth সার্ভিসটি ব্যবহার করতে পারি।

authStateChanges: এটি একটি Stream। এটি সবসময় দেখতে থাকে ব্যবহারকারী লগইন করল, নাকি লগ-আউট করল। এই Stream-এর ওপর ভিত্তি করেই আমরা স্ক্রিন পরিবর্তন করব।

signUpWithEmailPassword: এই ফাংশনটি ৩টি জিনিস নেয়: নাম, ইমেইল, পাসওয়ার্ড। প্রথমে createUserWithEmailAndPassword দিয়ে ইউজার তৈরি করে, তারপর updateDisplayName দিয়ে ইউজারের নামটি তার প্রোফাইলে সেভ করে।

signInWithEmailPassword: এটি ইমেইল ও পাসওয়ার্ড দিয়ে লগইন করতে সাহায্য করে।

signOut: ব্যবহারকারীকে লগ-আউট করে দেয়।

currentUser: বর্তমানে কোন ইউজার লগইন করা আছে তার তথ্য দেয়।
....................................................D:\CodesApplication\Flutter Projects\FlutterFireBlog\lib\screens\auth_gate.dart...
AuthGate কী?AuthGate বা Authentication Gate (অনেক সময় AuthWrapper বা DecisionTree নামেও পরিচিত) হলো একটি বিশেষ উইজেট, যা অ্যাপ চালু হওয়ার পর ব্যবহারকারীর অথেনটিকেশন স্ট্যাটাস (authentication status) চেক করে। সহজ কথায়, এটি সিদ্ধান্ত নেয় ব্যবহারকারীকে কোন স্ক্রিন দেখানো হবে।এর মূল কাজ হলো:•ব্যবহারকারী কি লগইন করা অবস্থায় আছেন? (Is the user logged in?)•নাকি লগইন করা নেই? (Is the user logged out?)এই দুটি অবস্থার উপর ভিত্তি করে এটি ব্যবহারকারীকে সঠিক স্ক্রিনে পাঠায়।
যদি লগইন করা থাকে, তাকে HomeScreen (যা আমরা পরের ধাপে বানাব) দেখাবে।

যদি লগইন করা না থাকে, তাকে LoginOrSignupScreen দেখাবে।

AuthGate একটি StreamBuilder ব্যবহার করে। এটি AuthService-এর authStateChanges নামক স্ট্রিমকে শোনে।

snapshot.hasData true হওয়ার মানে হলো User অবজেক্ট পাওয়া গেছে, অর্থাৎ ব্যবহারকারী লগইন করা আছেন। তখন তাকে ডেমো HomeScreen দেখানো হচ্ছে।

snapshot.hasData false হওয়ার মানে হলো ব্যবহারকারী লগইন করা নেই। তখন তাকে LoginOrSignupScreen দেখানো হচ্ছে।

LoginOrSignupScreen একটি স্টেটফুল উইজেট যা showLoginPage নামে একটি bool ভেরিয়েবল দিয়ে ঠিক করে কোনটি দেখাবে। togglePages ফাংশনটি কল করে এই bool-এর মান উল্টে দেওয়া হয়।


LoginScreen-এ onTapToSignUp প্যারামিটারের মাধ্যমে togglePages ফাংশনটি পাস করা হয়েছে।

SignUpScreen-এ onTapToLogin প্যারামিটারের মাধ্যমে togglePages ফাংশনটি পাস করা হয়েছে।

এই কৌশলটির মাধ্যমে, চাইল্ড উইজেটে (Login/Signup Screen) বাটনে ক্লিক হলে পেরেন্ট উইজেটের (LoginOrSignupScreen) showLoginPage স্টেট পরিবর্তন হয় এবং UI সফলভাবে টগল করে।
///////////////////////////////D:\CodesApplication\Flutter Projects\FlutterFireBlog\lib\models\post_model.dart/////////////////////////////////////////////////
Post ক্লাসটি আমাদের ডেটা মডেল।

toMap(): এই মেথডটি আমাদের Post অবজেক্টকে একটি Map-এ পরিণত করে, যা Firestore সহজেই বুঝতে পারে এবং সেভ করতে পারে।

fromMap(): এটি একটি factory constructor। যখন আমরা Firestore থেকে ডেটা পড়ব, তখন ডেটা Map হিসেবে আসবে। এই মেথডটি সেই Map-কে একটি Post অবজেক্টে পরিণত করবে।
.................................................................
//////////////////////////////////D:\CodesApplication\Flutter Projects\FlutterFireBlog\lib\services\firestore_service.dart//////////////
FirebaseFirestore.instance.collection('posts'): এটি আমাদের বলে দেয় যে আমরা Firestore-এর ভেতরের 'posts' নামের কালেকশনটির সাথে কাজ করতে চাই।

createPost ফাংশন:

এটি একটি title, content এবং ঐচ্ছিক imageUrl নেয়।

AuthService থেকে currentUser-এর তথ্য (বিশেষ করে uid এবং displayName) নেয়।

একটি নতুন Post অবজেক্ট তৈরি করে।

posts.add(newPost.toMap()) কমান্ডের মাধ্যমে সেই অবজেক্টকে Map-এ রূপান্তর করে Firestore-এ সেভ করে দেয়।
............................................................

AuthGate-এর ভেতরে LoginOrSignupScreen থাকে। তার কাজ হলো লগইন এবং সাইন-আপ স্ক্রিনের মধ্যে টগল করা। স্ক্রিনের ভেতরের বাটন (যেমন: "Sign Up" বাটন) ক্লিক হলে এখন আর সে সরাসরি Navigator.push করবে না। বরং, AuthGate থেকে পাস করা togglePages ফাংশনটিকে কল করবে।

...............................................................................
main.dart (শুরু)প্রজেক্টটি MyApp-এ শুরু হয়, এবং home হিসেবে AuthGate() সেট করা থাকে।২. AuthGate (প্রবেশদ্বার)AuthGate একটি রিয়েল-টাইম স্ট্রীম (AuthService().authStateChanges) ব্যবহার করে ইউজার লগইন আছে কি নেই তা ট্র্যাক করে।অবস্থাStreamBuilder এর রেজাল্টদেখানো স্ক্রিনলগইন নেইsnap.hasData = falseLoginOrSignupScreen()লগইন আছেsnap.hasData = true (User অবজেক্ট)HomeScreen()৩. LoginOrSignupScreen (টগলার)যেহেতু শুরুতে লগইন নেই, তাই AuthGate দেখায় LoginOrSignupScreen()। এই স্ক্রিনটি হল স্টেট ম্যানেজার। এর কাজ হলো:স্টেট (showLoginPage) অনুযায়ী একবার LoginScreen এবং একবার SignUpScreen লোড করা।স্ক্রিন দুটিতে টগল করার জন্য togglePages() ফাংশনটি পাস করা।৪. LoginScreen বা SignUpScreen (অ্যাকশন)ইউজার যখন LoginScreen-এ তার ক্রেডেনশিয়াল দিয়ে "Log In" বাটনে ক্লিক করে, তখন:_loginUser() ফাংশনটি চলে।AuthService().signInWithEP(...) কল হয়।Firebase এই ইউজারকে লগইন করায়।৫. ফিরে যাওয়া AuthGate-এ (রিয়েল-টাইম রিঅ্যাকশন)লগইন সফল হওয়ার সাথে সাথেই:AuthService().authStateChanges স্ট্রীমটি একটি নতুন ইভেন্ট (User অবজেক্ট) ইমিট করে।AuthGate-এর StreamBuilder রিয়েল-টাইমে তা ধরে ফেলে।AuthGate রিবিল্ড হয় এবং দেখতে পায় এখন snap.hasData = true।ফলে, AuthGate দ্রুত LoginOrSignupScreen() কে সরিয়ে দিয়ে HomeScreen() লোড করে দেয়।এইভাবে, লগইন/সাইন-আপ স্ক্রিন দুটি সরাসরি HomeScreen-এ নেভিগেট না করেও, AuthGate-এর মাধ্যমে পুরো অ্যাপ্লিকেশনের ফ্লো নিয়ন্ত্রিত হয়।আপনি এখন এই লজিকটি বুঝতে পেরেছেন। এটিই Flutter-এ Authentication ফ্লো হ্যান্ডেল করার সবচেয়ে পরিষ্কার ও আধুনিক উপায়। আপনি এখন ধাপ ৪ এ যেতে পারেন।
